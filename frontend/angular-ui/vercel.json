{
  "version": 2,
  "builds": [
    {
      "src": "frontend/angular-ui/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/angular-ui"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
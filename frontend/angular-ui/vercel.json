{
  "buildCommand": "cd frontend/angular-ui && npm install && npm run build",
  "outputDirectory": "frontend/angular-ui/dist/angular-ui",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}